<resources>
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/brown_bg</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/brithwine_red</item>
        <item name="actionMenuTextColor">@color/violet</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <item name="colorPrimary">@color/brown_bg</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/brithwine_red</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="windowNoTitle">true</item>
        <item name="android:itemBackground">@color/brithwine_bg</item>
        <item name="android:navigationBarColor">@color/black</item>

        <!-- Lable color in TRUE state and bar color FALSE and TRUE state. -->

        <item name="colorControlNormal">@color/grey</item>
        <item name="colorControlActivated">@color/grey</item>
        <item name="android:textColorPrimary">@color/grey</item>
    </style>

    <style name="TextLabelNew" parent="Theme.AppCompat">
        <!-- Hint color and label color in FALSE state. -->
        <item name="android:textColorHint">@color/grey</item>
        <item name="android:textSize">@dimen/sp15</item>
        <item name="android:textColor">@color/red</item>

        <!-- Lable color in TRUE state and bar color FALSE and TRUE state. -->
        <item name="colorAccent">@color/grey</item>
        <item name="colorControlNormal">@color/grey</item>
        <item name="colorControlActivated">@color/grey</item>
        <item name="android:textColorPrimary">@color/grey</item>
        <item name="colorControlHighlight">@color/edittext_grey</item>
    </style>

    <style name="TextLabel" parent="Theme.AppCompat">
        <!-- Hint color and label color in FALSE state. -->
        <item name="android:textColorHint">@color/edittext_grey</item>
        <item name="android:textSize">@dimen/sp16</item>
        <item name="android:textColor">@color/white</item>

        <!-- Lable color in TRUE state and bar color FALSE and TRUE state. -->
        <item name="colorAccent">@color/red</item>
        <item name="colorControlNormal">@color/grey</item>
        <item name="colorControlActivated">@color/grey</item>
        <item name="android:textColorPrimary">@color/grey</item>
        <item name="colorControlHighlight">@color/edittext_grey</item>
    </style>

</resources>