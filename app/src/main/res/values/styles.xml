<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/brown_bg</item>
        <item name="colorPrimaryDark">@color/brown_bg</item>
        <item name="colorAccent">@color/brithwine_red</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi = "lollipop">@color/edittext_grey</item>
        <item name="android:navigationBarColor" tools:targetApi="lollipop">@color/white</item>
    </style>

    <style name="TextLabelNew" parent="Theme.AppCompat">
        <!-- Hint color and label color in FALSE state. -->
        <item name="android:textColorHint">@color/grey</item>
        <item name="android:textSize">@dimen/sp16</item>
        <item name="android:textColor">@color/red</item>

        <!-- Lable color in TRUE state and bar color FALSE and TRUE state. -->
        <item name="colorAccent">@color/grey</item>
        <item name="colorControlNormal">@color/grey</item>
        <item name="colorControlActivated">@color/grey</item>
        <item name="android:textColorPrimary">@color/grey</item>
        <item name="colorControlHighlight">@color/edittext_grey</item>
    </style>

    <style name="TextLabel" parent="Theme.AppCompat">
        <!-- Hint color and label color in FALSE state. -->
        <item name="android:textColorHint">@color/edittext_grey</item>
        <item name="android:textSize">@dimen/sp16</item>
        <item name="android:textColor">@color/white</item>

        <!-- Lable color in TRUE state and bar color FALSE and TRUE state. -->
        <item name="colorAccent">@color/red</item>
        <item name="colorControlNormal">@color/grey</item>
        <item name="colorControlActivated">@color/grey</item>
        <item name="android:textColorPrimary">@color/grey</item>
        <item name="colorControlHighlight">@color/edittext_grey</item>
    </style>

    <style name="MyProgressDialogTheme" parent="ThemeOverlay.AppCompat.Dialog">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/transparent</item>
        <item name="colorAccent">@color/black</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="BtnClick">
        <item name="android:paddingTop">@dimen/dp12</item>
        <item name="android:paddingStart">@dimen/dp30</item>
        <item name="android:paddingEnd">@dimen/dp30</item>
        <item name="android:paddingBottom">@dimen/dp12</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">@dimen/dp200</item>
        <item name="android:background">@drawable/rounded_gredient_button_bg</item>
        <item name="android:foreground">@drawable/custome_rounded_ripple</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textSize">@dimen/sp16</item>
    </style>

    <style name="myDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <style name="EditTextTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/edittext_grey</item>
        <item name="colorControlActivated">@color/edittext_grey</item>
        <item name="colorControlHighlight">@color/edittext_grey</item>
    </style>

</resources>